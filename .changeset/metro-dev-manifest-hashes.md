---
'@module-federation/metro': patch
---

Move Metro manifest hashing into bundle serialization and update dev manifest generation so hashes are available after Metro lazily compiles exposed and shared modules.
